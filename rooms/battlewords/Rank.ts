export class Rank {
  private users = [];
  public validate(user, retry = 1) {
    // this.users.push(user);
    // if (this.users.length < 2) return true;
    // const [oldUser, newUser] = this.users;
    // const diff = (oldUser.xp * 10 * retry) / 100;
    // if (oldUser.xp < newUser.xp - diff || oldUser.xp > newUser.xp + diff) {
    //   return false;
    // }
    return true;
  }
}
